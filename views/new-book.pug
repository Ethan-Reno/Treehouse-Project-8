extends layout

block content
  h2= title
  
  form(action="/books", method="post")
    p
      label(for="title") Title:
      input(name="title" type="text" id="title" value=book.title autofocus)
    p
      label(for="author") Author:
      input(name="author" type="text" id="author" value=book.author)
    p
      label(for="genre") Genre:
      input(name="genre" type="text" id="genre" value=book.genre)
    p
      label(for="year") Year:
      input(name="year" type="text" id="year" value=book.year)
    input(type="submit")
    
    p 
      a.button(href="/books") Back